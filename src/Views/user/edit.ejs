<!DOCTYPE html>
<html lang="en">

<%- include("../partials/head") %>

    <body>
        <%- include("../partials/header") %>

        <main class="register" >
            <div class="form-title">
                <h2>Editar Perfil</h2>
            </div>
            <div class="login-register">
                    <div class="login-img">
                        <img src="/images/brand/PapaHoe-ELEMENTS-Lalinea.png" alt="LaLinea">
                    </div>
                    <form class="login-formulario" action="/user/edit/<%= user.id%>?_method=PUT" method="POST" enctype="multipart/form-data">
                        <section class="datos-section">
                            <h4>Datos Personales</h4>

                            <div>
                                <div class="login-input">
                                    <label for="nombre">Nombre</label>
                                    <input type="text" name="nombre" id="nombre" placeholder="Nombre" value="<%=user.nombre%>" required>
                                </div>
                                <div class="login-input">
                                    <label for="apellido">Apellido</label>
                                    <input type="text" name="apellido" id="apellido" placeholder="Apellido" value="<%=user.apellido%>" required>
                                </div>
                            </div>

                        <div class="login-input">
                            <label for="usuario">Usuario</label>
                            <input type="text" name="usuario" id="usuario" placeholder="Nombre de usuario" value="<%=user.usuario%>" required>
                        </div>
                        <div class="login-input">
                            <label for="email">Email</label>
                            <input type="email" name="email" id="email" placeholder="Email" value="<%=user.email%>" required>
                        </div>
                        <div class="login-input hidden">
                            <label for="contraseña">Contraseña</label>
                            <input type="password" name="password" id="password" placeholder="Contraseña" value="<%=user.password%>" required>
                        </div>
                    </section>
                        <section class="datos-section">
                            <h4>Datos de Envío</h4>
                            <div class="login-input">
                                <label for="telefono">Teléfono</label>
                                <input type="text" name="telefono" id="telefono" placeholder="Teléfono" value="<%=user.telefono%>" required>
                            </div>
                            <div class="login-input">
                                <label for="direccion">Dirección</label>
                                <input type="text" name="direccion" id="direccion" placeholder="Dirección" value="<%=user.direccion%>" required>
                            </div>
                            <div class="direccion">
                                <div class="login-input">
                                    <label for="departamento">Piso, departamento</label>
                                    <input type="text" name="departamento" id="departamento" placeholder="Piso, departamento" value="<%=user.departamento%>">
                                </div>
                                <div class="login-input">
                                    <label for="localidad">Localidad</label>
                                    <input type="text" name="localidad" id="localidad" placeholder="Localidad" value="<%=user.localidad%>" required>
                                </div>
                                <div class="login-input">
                                    <label for="codigoPostal">Código Postal</label>
                                    <input type="text" name="codigoPostal" id="codigoPostal" placeholder="Código Postal" value="<%=user.codigoPostal%>" required>
                                </div>
                            </div>
                        </section>
                        <br>

                        <!--Aquí solicito la imagen-->
                        <div class="custom-file">
                            <figure class="login-input">
                                <img src="/img/<%= user.avatar%>" alt="">
                            </figure>
                            <input type="hidden"  name="oldImagen"  value="<%= user.avatar%>">
                            <!--De esta forma logro conservar y que viaje la imagen anterior de esta forma evito que el usuario seleccione una imagen nuevamente, ( si no lo desea) -->
                            <input type="file" name="avatar" id="avatar">                            
                            <label for="avatar">Elige una imagen de perfil</label>
                        </div>

                        <div class="submit-button">
                            <button class="submit-button" type="submit">Editar</button>
                        </div>
                    </form>
                </div>
            </main>
            <%- include("../partials/footer") %>
    </body>

</html>