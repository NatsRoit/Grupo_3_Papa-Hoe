<!DOCTYPE html>
<html lang="en">

<%- include("../partials/head") %>

    <body>
        <%- include("../partials/header") %>


            <header class="section-header">
                <img id="carrousel-image-1" src="/images/stock/guille-unsplash.jpg" alt="PapaHoe foto1">
            </header>

            <main class="profile">
                <section class="main-container">
                    <div class="left-wrapper">
                        <div class="img-slider">
                            <img src="/images/usuarios/<%= user.avatar %>" alt="imagen de perfil">
                        </div>
                    </div>

                    <div class="info-wrapper">
                        <div>
                            <br>
                            <h2><%= 'Hola ' + user.nombre + ' ' + user.apellido + ' !'%> </h2>
                        </div>

                        <div>
                            <p><strong>Nombre</strong>: <%= user.nombre%> <%= user.apellido%></p>
                            <p><strong>E-mail</strong>: <%= user.email%></p>
                            <p><strong>Teléfono</strong>: <%= user.telefono%></p>
                            <p><strong>Dirección</strong>: <%= user.direccion + ', ' +  user.departamento + '. ' + user.localidad + " \(" + user.codigoPostal + "\)"%></p>
                            <br><br>
                            <hr>
                            <br><br>
                        </div>
                        <div>
                            <a href="/user/edit/<%= user.id%>">EDITAR</a>
                        </div>
                    </div>
                </section>

            </main>
            <%- include("../partials/footer") %>
    </body>

</html>